---
title: PDF 파서 믹스인 참조 복구
date: 2026-09-25
area: [pipeline]
decision: a35b1d5 리팩터링 뒤 끊긴 FulltextParser 참조·_looks_like_numbered_heading·Counter import를 복구하고, 세 파싱 경로의 반환 source를 단위 테스트로 고정한다
---

**결정과 근거** — 기준선(143cc72) 감사에서 `src/integrations/pdf_parser/`의 믹스인이 import하지 않은 `FulltextParser`를
참조하고, a35b1d5 리팩터링에서 `_looks_like_numbered_heading`이 빠졌으며, `layout_parser.py`에 `Counter` import가 없다는
것을 확인했다. 레이아웃 파서·pypdf·초록 폴백 중 어느 경로로 들어가도 예외로 끝났다. 동작을 새로 설계하지 않고 참조만
복구했고(dc0981d), 경로마다 어떤 `source`(`layout_pdf`, `pdf`, `fallback_abstract`)를 돌려주는지를
`tests/unit/test_pdf_parser.py`로 고정했다.

**트레이드오프** — 믹스인 분할 구조를 다시 짜는 대안이 있었지만 복구가 목적이라 범위를 참조 복구와 테스트로 좁혔다.
한 믹스인이 다른 믹스인의 메서드를 가정하는 암묵적 의존은 그대로 남는다. HURIDOCS 레이아웃 파서 경로는 CI에서 실제
서버 없이 가짜 응답으로만 검증한다.

**eval 영향** — `test_pdf_parser.py`의 파서 테스트 14개가 기준선 코드에서 실패하고 수정 뒤 통과한다. 로컬 코퍼스의 청크
15,897개가 모두 이 파싱 경로를 거쳐 만들어졌으므로 이후 검색·생성 평가 전체의 전제다.

**알려진 한계** — 로컬 코퍼스는 GPU 파서 없이 pypdf로만 만들었다(fulltext source 275건 모두 `pdf`). `layout_pdf` 경로가
만든 청크의 품질은 실측하지 않았다.

**트러블슈팅** — 모든 PDF 파싱 경로가 예외로 끝남

- 증상: prepare 단계에서 어떤 논문이든 본문 파싱이 `NameError` 또는 `AttributeError`로 실패했다.
- 원인: a35b1d5 리팩터링에서 파서를 믹스인으로 나누면서 `FulltextParser` import, `_looks_like_numbered_heading` 메서드,
  `layout_parser.py`의 `Counter` import가 빠졌다.
- 확인 방법: 기준선 코드에 `tests/unit/test_pdf_parser.py`를 적용해 파서 테스트 14개가 모두 실패하는 것을 확인했다.
- 해결: 누락된 참조 세 가지를 복구했다(dc0981d). 같은 테스트 14개가 통과하고, `test_numbered_heading_helper_restored`가
  복구한 메서드를 직접 호출한다.
- 재발 방지: CI python 잡이 매 push에 `ruff check`(F 규칙이 정의되지 않은 이름을 잡는다)와 `pytest tests/unit`을 돈다.
